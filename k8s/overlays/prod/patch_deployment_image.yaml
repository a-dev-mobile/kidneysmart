apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-log-deployment
  namespace: kidneysmart
spec:
  template:
    spec:
      containers:
      - name: api-log
        image: wayofdt/kidneysmart-api-log:latest
